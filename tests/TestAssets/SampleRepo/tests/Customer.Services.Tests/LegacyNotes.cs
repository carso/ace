namespace Customer.Services.Tests
{
    // Deliberately malformed C# — used as a parser resilience fixture (SRS §17).
    public class LegacyNotes
    {
        public void Broken(
        {
            var x = ;
            if (x
    }
