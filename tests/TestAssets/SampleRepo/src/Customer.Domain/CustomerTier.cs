namespace Customer.Domain
{
    /// <summary>Loyalty tier of a customer (block-scoped namespace on purpose, as a parse fixture).</summary>
    public enum CustomerTier
    {
        Standard,
        Silver,
        Gold,
        Platinum,
    }
}
